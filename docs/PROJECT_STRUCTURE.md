# 🎰 数字抽取转盘游戏 - 项目结构说明

## 📁 目录结构

```
├── .lovable/
│   └── plan.md                 # 项目计划和功能说明文档
│
├── docs/
│   └── PROJECT_STRUCTURE.md    # 本文档
│
├── public/
│   ├── favicon.ico             # 网站图标
│   ├── placeholder.svg         # 占位图片
│   └── robots.txt              # 搜索引擎爬虫规则
│
├── src/
│   ├── assets/                 # 静态资源文件夹
│   │
│   ├── components/             # React 组件
│   │   ├── ui/                 # shadcn/ui 基础组件库
│   │   │   ├── button.tsx      # 按钮组件
│   │   │   ├── card.tsx        # 卡片组件
│   │   │   ├── input.tsx       # 输入框组件
│   │   │   ├── toast.tsx       # 提示消息组件
│   │   │   └── ...             # 其他 UI 组件
│   │   │
│   │   ├── NavLink.tsx         # 导航链接组件
│   │   ├── NumberCard.tsx      # 🎱 台球数字卡片组件（含长按消除功能）
│   │   ├── NumberWheel.tsx     # 🎡 转盘组件（抽取动画）
│   │   └── PlayerCard.tsx      # 👤 玩家卡片组件（展示分配结果）
│   │
│   ├── hooks/                  # 自定义 React Hooks
│   │   ├── use-mobile.tsx      # 移动端检测 Hook
│   │   └── use-toast.ts        # Toast 提示 Hook
│   │
│   ├── lib/
│   │   └── utils.ts            # 工具函数（cn 类名合并等）
│   │
│   ├── pages/                  # 页面组件
│   │   ├── Index.tsx           # 🏠 主页面（游戏核心逻辑）
│   │   └── NotFound.tsx        # 404 页面
│   │
│   ├── test/                   # 测试文件
│   │   ├── example.test.ts     # 示例测试
│   │   └── setup.ts            # 测试配置
│   │
│   ├── App.css                 # 应用样式
│   ├── App.tsx                 # 应用入口组件（路由配置）
│   ├── index.css               # 🎨 全局样式（含台球颜色定义）
│   ├── main.tsx                # 应用入口文件
│   └── vite-env.d.ts           # Vite 类型声明
│
├── components.json             # shadcn/ui 配置
├── eslint.config.js            # ESLint 配置
├── index.html                  # HTML 入口
├── package.json                # 项目依赖配置
├── postcss.config.js           # PostCSS 配置
├── tailwind.config.ts          # Tailwind CSS 配置
├── tsconfig.json               # TypeScript 配置
├── vite.config.ts              # Vite 构建配置
└── vitest.config.ts            # Vitest 测试配置
```

## 🎯 核心组件说明

### 页面组件

| 文件 | 说明 |
|------|------|
| `src/pages/Index.tsx` | 主页面，包含游戏核心逻辑：抽取、分配、重置 |

### 功能组件

| 文件 | 说明 |
|------|------|
| `src/components/NumberWheel.tsx` | 转盘组件，展示 1-15 数字，点击触发抽取动画 |
| `src/components/PlayerCard.tsx` | 玩家卡片，展示玩家名称和分配到的数字 |
| `src/components/NumberCard.tsx` | 数字卡片，采用台球配色，支持长按消除 |

## 🎨 样式系统

- **Tailwind CSS**: 原子化 CSS 框架
- **shadcn/ui**: 基于 Radix UI 的组件库
- **台球配色**: 在 `src/index.css` 中定义 `.ball-1` 至 `.ball-15`

### 台球颜色对照

| 编号 | 类型 | 颜色 |
|------|------|------|
| 1 | 全色球 | 黄色 |
| 2 | 全色球 | 蓝色 |
| 3 | 全色球 | 红色 |
| 4 | 全色球 | 紫色 |
| 5 | 全色球 | 橙色 |
| 6 | 全色球 | 绿色 |
| 7 | 全色球 | 棕色 |
| 8 | 全色球 | 黑色 |
| 9-15 | 花色球 | 白底 + 对应色条纹 |

## 🛠️ 技术栈

- **框架**: React 18 + TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **UI 组件**: shadcn/ui
- **路由**: React Router DOM
- **状态管理**: React Hooks (useState, useCallback)
- **测试**: Vitest

## 📱 响应式适配

项目已针对移动端进行优化：
- 转盘尺寸自适应屏幕宽度
- 玩家卡片在移动端垂直堆叠
- 数字卡片尺寸和间距适配小屏幕
- 触控友好的交互设计
